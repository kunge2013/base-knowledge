- Capabilities 能力
	- [AI Platform (AIP)](https://www.palantir.com/docs/foundry/aip/overview/)
		- [Data connectivity & integration](https://www.palantir.com/docs/foundry/data-integration/overview/)
		- [Model connectivity & development](https://www.palantir.com/docs/foundry/model-integration/overview/)
		- [Ontology building](https://www.palantir.com/docs/foundry/ontology/overview/)
		- [Developer toolchain](https://www.palantir.com/docs/foundry/dev-toolchain/overview/)
		- [Use case development](https://www.palantir.com/docs/foundry/app-building/overview/)
		- [Observability](https://www.palantir.com/docs/foundry/observability/overview/)
		- [Analytics](https://www.palantir.com/docs/foundry/analytics/overview/)
		- [Product delivery](https://www.palantir.com/docs/foundry/devops/overview/)
		- [Security & governance](https://www.palantir.com/docs/foundry/security/overview/)
		- [Management & enablement](https://www.palantir.com/docs/foundry/administration/overview/)
- [Getting started 入门指南](https://www.palantir.com/docs/foundry/getting-started/overview/)
- [Architecture center 建筑中心](https://www.palantir.com/docs/foundry/architecture-center/overview/)
- Platform updates 平台更新

[Ontology building](https://www.palantir.com/docs/foundry/ontology/overview/) Object and link types [Allow users to edit objects and links](https://www.palantir.com/docs/foundry/object-link-types/allow-editing/)

## Allow users to edit objects and links允许用户编辑对象和链接。

## Editing data from Foundry object applications编辑来自 Foundry 对象应用程序的数据

You can allow users in user applications (like Workshop and Object Views) to edit property values, add and remove links, and create and delete objects. You can also configure side effects (like notifications) based on edits made by users.  
您可以允许用户应用程序中的用户编辑属性值、添加或删除链接，以及创建或删除对象。此外，还可以根据用户所做的修改来配置相应的副作用效果（如通知）。

The supported way to configure this functionality is to create and configure action types in the Ontology Manager. [Learn more about how to set up action types.](https://www.palantir.com/docs/foundry/action-types/overview/)  
支持的配置方式是在 Ontology Manager 中创建和配置动作类型。更多关于如何设置动作类型的详细信息，请参考相关文档。

The remainder of this documentation covers what needs to be configured on object types and link types before users can take actions.  
其余的文档内容则涉及在用户能够执行操作之前，需要对对象类型和链接类型进行哪些配置。

## Editing data from external applications编辑来自外部应用程序的数据

The [Objects API](https://www.palantir.com/docs/foundry/api/ontology-resources/actions/apply-action/) provides endpoints for external clients to write and update objects, properties, and links with full permissions enforcement.  
Objects API 提供了一些端点，供外部客户端进行对象的写入和更新操作，同时还能修改属性和链接，并且能够实现完整的权限控制。

## Set up the prerequisites准备好所有必要的前提条件。

In order for a user to be able to take an action defined in an action type configuration, a writeback dataset must be created. The writeback dataset will read the edits made by users when it is built and will reflect the most up-to-date state of any given object.  
为了让用户能够执行在动作类型配置中定义的操作，必须创建一个回写数据集。该数据集会在创建时读取用户所做的编辑操作，从而反映出某个对象的最新状态。

Note that edits are written to the writeback dataset and not the dataset backing an object type or link type. This ensures that users have access to both the original data and the edited data in their analyses.  
请注意，编辑内容会被写入回写数据集中，而不是存储于存储对象类型或链接类型的数据集中。这样就能确保用户能够同时访问原始数据以及经过编辑的数据。

To set up a writeback dataset:  
要创建一个可以写入数据的数据集：

1. Navigate to the **Datasources** page of the object type or link type you want to enable edits on.  
	请前往您想要启用编辑操作的对象类型或链接类型的数据源页面。
2. Select **Generate** in the **Writeback dataset** portion of the page to create a new writeback dataset. A dialog will open asking you to choose a Project where you’d like to place the dataset. Select a location.  
	在页面的“Writeback 数据集”部分选择“Generate”，以创建一个新的写回数据集。将会出现一个对话框，要求您选择想要放置该数据集的项目。请选择一个位置。
3. Make sure the users who you want to be able to edit the object type or link type have edit permissions on the writeback dataset.  
	请确保那些想要编辑该对象类型或链接类型的用户，拥有对该写回数据集的编辑权限。
4. Ensure that the users who you want to be able to view changes made to the object type or link type have view permissions on the writeback dataset.  
	确保那些想要查看该对象类型或链接类型所发生变化的用户，拥有对该写回数据集的查看权限。
	- The ability to view objects and links is controlled by an object type and link type’s backing datasources.  
		查看对象和链接的能力取决于对象的类型以及链接类型所依赖的数据源。
		- The ability to view the edits on objects and links is controlled by the permissions on the writeback dataset.  
		能够查看对象和链接的编辑内容，这一功能取决于对写回数据集的权限设置。
		- If users have access to only the former, they can see only the object as it exists without edits applied. If users have access to the latter, they can see both the edits and the object as it exists at present.  
		如果用户只能访问前者，那么他们只能看到未经编辑的原始对象。而如果用户能够访问后者，那么他们就可以同时看到编辑后的结果以及当前实际的对象状态。

If you want a property in your object type to capture manually-entered data from an end user (via an Action or other writeback method) and this data does not yet exist in Foundry, you will need to add an empty column to the object type’s backing dataset and map it to a new property in your object type. You also need to enable edits; this is done by creating a writeback dataset in Object Storage v1 or by toggling on edits in Object Storage v2.  
如果您希望对象类型中的某个属性能够捕获来自最终用户手动输入的数据（通过动作或其他写入方法传递），而这些数据在 Foundry 中还不存在，那么您需要向该对象类型的后台数据集中添加一列空字段，并将其映射到对象类型中的新属性上。此外，您还需要启用编辑功能；这可以通过在 Object Storage v1 中创建写入数据集来实现，或者在 Object Storage v2 中开启编辑功能来实现。

Object and link types • Allow users to edit objects and links • Palantir