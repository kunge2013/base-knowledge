- Capabilities 能力
	- [AI Platform (AIP)](https://www.palantir.com/docs/foundry/aip/overview/)
		- [Data connectivity & integration](https://www.palantir.com/docs/foundry/data-integration/overview/)
		- [Model connectivity & development](https://www.palantir.com/docs/foundry/model-integration/overview/)
		- [Ontology building](https://www.palantir.com/docs/foundry/ontology/overview/)
		- [Developer toolchain](https://www.palantir.com/docs/foundry/dev-toolchain/overview/)
		- [Use case development](https://www.palantir.com/docs/foundry/app-building/overview/)
		- [Observability](https://www.palantir.com/docs/foundry/observability/overview/)
		- [Analytics](https://www.palantir.com/docs/foundry/analytics/overview/)
		- [Product delivery](https://www.palantir.com/docs/foundry/devops/overview/)
		- [Security & governance](https://www.palantir.com/docs/foundry/security/overview/)
		- [Management & enablement](https://www.palantir.com/docs/foundry/administration/overview/)
- [Getting started 入门指南](https://www.palantir.com/docs/foundry/getting-started/overview/)
- [Architecture center 建筑中心](https://www.palantir.com/docs/foundry/architecture-center/overview/)
- Platform updates 平台更新

[Ontology building](https://www.palantir.com/docs/foundry/ontology/overview/) Object and link types [Properties](https://www.palantir.com/docs/foundry/object-link-types/properties-overview/) [Required properties](https://www.palantir.com/docs/foundry/object-link-types/required-properties/)

## Required properties 必需的属性

Required properties are object type properties that must have a value. You can use this object type property to validate that there are no objects that have a null value for this property, or an empty array if it is an array property. This validation applies to data from the backing datasource and edits via actions.  
必需的属性是指那些必须拥有值的对象类型属性。你可以利用这些属性来验证：没有任何对象的该属性值为空值，或者如果这是一个数组属性，那么数组内部也不为空。这种验证适用于来自底层数据源的数据以及通过操作进行的数据编辑操作。

## Summary of required properties所需属性的总结

When working with required properties, keep the following in mind:  
在处理必需的属性时，请记住以下几点：

- **Validation happens when data is being indexed into the object:** The check for null values happens as backing datasources are indexed into Object Storage. This means that the ontology modification itself will succeed if the column backing a required property contains null values.  
	验证发生在数据被索引到对象中的时候：当后台数据源被索引到对象存储中时，会检查空值的情况。这意味着，如果某个必需属性的对应列包含空值，那么对该属性的修改仍然可以成功执行。
- **Array properties cannot be empty:** Setting an array property to required ensures the presence of at least one item.  
	数组属性不能为空：将数组属性设置为必填项可以确保至少有一个元素存在。
- **Changes via actions are validated at apply time:** If you attempt to write a null or empty value to a property via an action, the action will fail to execute.  
	通过操作进行的变化在应用时会得到验证：如果你尝试通过操作将一个空值或空字符串写入某个属性，那么该操作将会失败。
- **Available only in Object Storage v2:** Required properties are only supported by object types leveraging Object Storage v2.  
	仅适用于 Object Storage v2 版本。一些必需的属性仅适用于使用 Object Storage v2 版本的对象类型。

### Create a required property创建一个必需的属性。

1. Navigate to **Ontology Manager**.  
	请前往 Ontology Manager 界面。
2. Choose the object type, and then the property you want to set as required.  
	选择对象类型，然后选择想要设置的属性。
3. Select **Create Property** and fill in the required details, including the property name, type, and description.  
	选择“创建属性”选项，然后填写所需的详细信息，包括属性名称、类型以及描述等。
4. Under the **Configuration** section, toggle on the **Required** toggle.  
	在“配置”部分下，打开“必需”选项。
5. **Save** your changes to the Ontology and wait for the reindex to be completed.  
	请保存对 Ontology 所做的更改，然后等待重新索引的整个过程完成。

Note that if there is any null value currently set on the backing column for the property, the reindex will fail. To fix this, you must either update the backing datasource to no longer have nulls in the column, or unset the property as required.  
请注意，如果当前在该属性的后台列中设置了空值，那么重新索引操作将会失败。为了解决这个问题，您必须更新后台数据源，使其不再在列中包含空值，或者根据需求取消对该属性的设置。

![Required property toggled in configuration pane.](https://www.palantir.com/docs/resources/foundry/object-link-types/required_property.png?width=500)

### Required properties that allow empty arrays允许使用空数组的必需属性

You can configure your required property to allow empty arrays. This means that the property will still reject null values, but will accept empty arrays. This is useful for mandatory control properties which can never be null, but can be configured to allow empty arrays. Learn more about [mandatory control properties](https://www.palantir.com/docs/foundry/object-link-types/mandatory-control-properties/).  
你可以配置所需的属性，使其能够接受空数组。这意味着该属性仍然会拒绝空值，但会接受空数组。这适用于那些永远不能为空值的强制性控制属性，不过可以配置为允许空数组。了解更多关于强制性控制属性的信息。

It is important to note that Actions will write an empty array to any property that is mapped to a parameter, but the parameter is not set. This means that if you have a required property that allows empty arrays, and you leave the parameter blank in an Action, the Action will succeed and write an empty array to the property. If you do not want this behavior and want to enforce that users always set a value for this property via Actions, you should not allow empty arrays on your required property.  
需要注意的是，当某个参数没有被设置时，Actions 会将该参数对应的属性设置为空数组。这意味着，如果某个必填属性允许出现空数组，而在 Action 中将该参数留空，那么 Action 仍然可以正常运行，并将空数组写入该属性。如果你不希望出现这种情况，并且希望用户总是通过 Actions 为该属性设置值，那么就不应该允许在必填属性中出现空数组。

![Advanced required property configuration](https://www.palantir.com/docs/resources/foundry/object-link-types/advanced_required_property.png?width=500)

### Required properties for object types with multiple backing datasources具有多个后台数据源的对象类型所需的属性

You may occasionally encounter situations where null values appear in required properties of object types backed by multiple datasources. This phenomenon occurs when a record for the object is present in some datasources, but absent in the datasource that supplies the required property.  
有时可能会遇到这样的情况：在由多个数据源支持的对象类型中，某些必需的属性出现空值。这种现象发生在某个记录存在于某些数据源中，但在提供该必需属性的数据源中却没有该记录的情况。

The following example illustrates this behavior. Assume there is a `Movie` object type with two backing datasources and a `Genre` property that is required.  
以下示例展示了这种行为。假设有一个 `Movie` 对象类型，它有两个后台数据源，还有一个必须是必填项的 `Genre` 属性。

**Datasource 1 数据来源 1**

| Movie Id 电影编号 | Title 标题 | Box office 票房收入 | Regions 地区/区域 |
| --- | --- | --- | --- |
| 101 | The Adventure Begins 冒险开始了 | 200m | \["USA", "Canada", "UK"\] \["美国", "加拿大", "英国"\] |
| 102 | Love in the City 城市中的爱 | 75m | \[\] |
| 103 | Galactic Battles 银河战争 | 500m | \["USA", "UK", "Australia"\]   \["美国", "英国", "澳大利亚"\] |

**Datasource 2 数据来源 2**

| Movie Id 电影编号 | Budget 预算 | Genre (Required) 类型（必填） | Director 导演 |
| --- | --- | --- | --- |
| 101 | 50m | Adventure 冒险 | Michael John Smith 迈克尔·约翰·史密斯 |
| 102 | 20m | Romance 浪漫故事 | Jane Doe 简·多伊 |
| 103 | 150m | Sci-Fi 科幻 |  |

If a new `Movie` is added to both backing datasources without providing a value for `Genre`, it will fail to index to the Ontology.  
如果在一个 backing data source 中添加了一个新的 `Movie` ，而该 `Movie` 没有为 `Genre` 提供任何值，那么该数据将无法被索引到 Ontology 中。

However, suppose only Datasource 1 had a row for the new `Movie` object.  
不过，假设只有 Datasource 1 有一个与新对象 `Movie` 相关的行。

**Datasource 1 数据来源 1**

| Movie Id | Title 标题 | Box office 票房收入 | Regions 地区/区域 |
| --- | --- | --- | --- |
| 101 | The Adventure Begins 冒险开始了 | 200m | \["USA", "Canada", "UK"\] |
| 102 | Love in the City 城市中的爱 | 75m | \[\] |
| 103 | Galactic Battles 银河战争 | 500m | \["USA", "UK", "Australia"\]   \["美国", "英国", "澳大利亚"\] |
| 104 | Happy Ending | 150m | \["UK", "FRANCE"\] |

**Datasource 2**

| Movie Id 电影编号 | Budget 预算 | Genre (Required) 类型（必填） | Director 导演 |
| --- | --- | --- | --- |
| 101 | 50m | Adventure 冒险 | Michael John Smith 迈克尔·约翰·史密斯 |
| 102 | 20m | Romance 浪漫故事 | Jane Doe 简·多伊 |
| 103 | 150m | Sci-Fi 科幻 |  |

The example above will successfully get indexed into the Ontology, despite the fact that the resulting object would have no value for the required property.  
上述示例能够成功地被索引到 Ontology 中，尽管生成的对象在所需的属性上并没有任何值。

| Property 财产 | Value 价值 |
| --- | --- |
| Movie Id 电影编号 | 104 |
| Title 标题 | Happy Ending 圆满的结局 |
| Box Office 票房情况 | 150m |
| Regions 地区/区域 | \["UK", "FRANCE"\] \["英国", "法国"\] |
| Budget 预算 |  |
| Genre (Required) 类型（必填） |  |
| Director 导演 |  |

The same applies to objects created via Action edits. `Movie` objects can be created or modified successfully, if they only contain properties tied to columns in `Datasource 1`. However if the Action adds a property to the object that is sourced from `Datasource 2`, such as `Budget`, then the Action will be invalid and will fail to execute. This is because the object will now be present on `Datasource 2` and thus `Genre` must be set.  
对于通过“操作编辑”方式创建的对象也是如此。如果这些对象仅包含与 `Datasource 1` 中的列相关的属性，那么就可以成功创建或修改它们。然而，如果操作为从 `Datasource 2` 中获取的对象添加了新的属性，比如 `Budget` ，那么操作就会失效，无法执行。这是因为此时对象会存在于 `Datasource 2` 中，而 `Genre` 必须被设置好。

Object and link types • Properties • Required properties • Palantir