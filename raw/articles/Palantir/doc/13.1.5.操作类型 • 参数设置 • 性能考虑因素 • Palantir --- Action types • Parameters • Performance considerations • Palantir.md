- Capabilities 能力
	- [AI Platform (AIP)](https://www.palantir.com/docs/foundry/aip/overview/)
		- [Data connectivity & integration](https://www.palantir.com/docs/foundry/data-integration/overview/)
		- [Model connectivity & development](https://www.palantir.com/docs/foundry/model-integration/overview/)
		- [Ontology building](https://www.palantir.com/docs/foundry/ontology/overview/)
		- [Developer toolchain](https://www.palantir.com/docs/foundry/dev-toolchain/overview/)
		- [Use case development](https://www.palantir.com/docs/foundry/app-building/overview/)
		- [Observability](https://www.palantir.com/docs/foundry/observability/overview/)
		- [Analytics](https://www.palantir.com/docs/foundry/analytics/overview/)
		- [Product delivery](https://www.palantir.com/docs/foundry/devops/overview/)
		- [Security & governance](https://www.palantir.com/docs/foundry/security/overview/)
		- [Management & enablement](https://www.palantir.com/docs/foundry/administration/overview/)
- [Getting started 入门指南](https://www.palantir.com/docs/foundry/getting-started/overview/)
- [Architecture center 建筑中心](https://www.palantir.com/docs/foundry/architecture-center/overview/)
- Platform updates 平台更新

[Ontology building](https://www.palantir.com/docs/foundry/ontology/overview/) [Action types](https://www.palantir.com/docs/foundry/action-types/overview/) [Parameters](https://www.palantir.com/docs/foundry/action-types/parameter-overview/) [Performance considerations](https://www.palantir.com/docs/foundry/action-types/parameter-performance-considerations/)  
ontology 构建、动作类型、参数、性能考量

## Performance considerations for parameter configuration参数配置的性能考量

Dependencies between parameters, such as in the definitions of [default values](https://www.palantir.com/docs/foundry/action-types/parameters-default-value/) and [multiple-choice options](https://www.palantir.com/docs/foundry/action-types/parameters-filter/), can impact the time that it takes for an action form to load. For example, consider the following action parameter configuration:  
参数之间的依赖关系，比如在默认值和多选选项的定义中，会影响到表单加载所需的时间。例如，考虑以下动作参数的配置：

1. The first parameter is an `object reference` with a `from single result of object set` default value.  
	第一个参数是一个包含 `from single result of object set` 默认值的 `object reference` 对象。
2. The second parameter is a string with a default value that is an `object parameter property` referencing the first parameter.  
	第二个参数是一个字符串，其默认值为一个 `object parameter property` ，该值用于引用第一个参数。
3. The third parameter is also a string, with no default value, but configured as a `multiple choice` dropdown using `get options from an object set`. The object set definition references the second parameter.  
	第三个参数也是一个字符串，没有默认值，但是通过 `get options from an object set` 进行了配置，具体格式为 `multiple choice` 。这个参数在对象定义中引用了第二个参数。

When a user loads an action form for this action, multiple operations need to be performed iteratively.  
当用户为这一操作加载操作表单时，需要依次执行多个操作。

1. First, the default value for the first parameter needs to be retrieved.  
	首先，需要获取第一个参数的默认值。
2. Then, the second parameter's default value needs to be derived from the first parameter value.  
	然后，第二个参数的默认值需要从第一个参数的值中推导得出。
3. Finally, the options for the third parameter need to be derived from the second parameter value.  
	最后，第三个参数的选项需要基于第二个参数的值来确定。

When configuring action parameters, it is recommended to keep the dependency hierarchy as flat as possible. In the context of the action described above, referencing the first parameter instead of the second parameter in the third parameter's object set definition would allow the necessary information for the second and third parameter to be derived in parallel, reducing the total latency between opening the form and the form being fully interactive.  
在配置动作参数时，建议尽量保持依赖层次结构的最简单形式。在上述动作的上下文中，如果引用第三个参数对象定义中的第一个参数而不是第二个参数，那么第二个和第三个参数的必要信息就可以并行获取，从而减少了从打开表单到表单完全可交互状态转变之间的总延迟时间。

Action types • Parameters • Performance considerations • Palantir