- Capabilities 能力
	- [AI Platform (AIP)](https://www.palantir.com/docs/foundry/aip/overview/)
		- [Data connectivity & integration](https://www.palantir.com/docs/foundry/data-integration/overview/)
		- [Model connectivity & development](https://www.palantir.com/docs/foundry/model-integration/overview/)
		- [Ontology building](https://www.palantir.com/docs/foundry/ontology/overview/)
		- [Developer toolchain](https://www.palantir.com/docs/foundry/dev-toolchain/overview/)
		- [Use case development](https://www.palantir.com/docs/foundry/app-building/overview/)
		- [Observability](https://www.palantir.com/docs/foundry/observability/overview/)
		- [Analytics](https://www.palantir.com/docs/foundry/analytics/overview/)
		- [Product delivery](https://www.palantir.com/docs/foundry/devops/overview/)
		- [Security & governance](https://www.palantir.com/docs/foundry/security/overview/)
		- [Management & enablement](https://www.palantir.com/docs/foundry/administration/overview/)
- [Getting started 入门指南](https://www.palantir.com/docs/foundry/getting-started/overview/)
- [Architecture center 建筑中心](https://www.palantir.com/docs/foundry/architecture-center/overview/)
- Platform updates 平台更新

[Ontology building](https://www.palantir.com/docs/foundry/ontology/overview/) [Action types](https://www.palantir.com/docs/foundry/action-types/overview/) [Parameters](https://www.palantir.com/docs/foundry/action-types/parameter-overview/) [Object dropdown security considerations](https://www.palantir.com/docs/foundry/action-types/dropdown-security/)  
ontology 构建过程中涉及的动作类型、参数设置、对象选择下拉菜单功能，以及相关的安全考量问题。

## Object dropdown security considerations对象下拉菜单的安全考虑因素

Static value filters in object dropdown validations are exposed to all users who can view the action type. Use of these filters risks exposing property value combinations to users without permissions to view the filtered objects. This risk is mitigated by relying on object properties or parameters to filter the object set. The values are not directly visible in the interface.  
在对象下拉菜单中的静态值过滤功能，会暴露给所有能够查看该操作类型的用户。使用这种过滤方式可能会导致没有权限查看过滤后对象的使用者看到这些属性值组合。不过，可以通过依赖对象属性或参数来进行过滤，这样就不会有用户能够直接看到这些值。

## Example: Data privacy issue例如：数据隐私问题

As an example, imagine we have a `Document` object with an `Investigation Name` property. In our action type, we add a filter on the object reference parameter to only show **Documents** where **Investigation Name** is `Area 51 Investigation`.  
例如，假设我们有一个包含 `Investigation Name` 属性的 `Document` 对象。在我们的操作中，我们会添加一个过滤器，只显示那些调查名称为 `Area 51 Investigation` 的文档。

![Object Dropdown Security Concern](https://www.palantir.com/docs/resources/foundry/action-types/objectDropdownSecurityFilter.png)

Here, we would potentially be revealing that `Area 51 Investigation` is a property value of some `Document` objects to users who cannot view those documents.  
在这里，我们可能会向那些无法查看那些文档的用户透露， `Area 51 Investigation` 是某些 `Document` 对象的属性值。

This only applies to **static value filters**. There is no reference to the `Area 51 Investigation` when filtering the `Investigation Name` property by a parameter or by the property of another object because:  
这仅适用于静态值过滤器。在通过参数或另一个对象的属性来过滤 `Investigation Name` 属性时，并没有涉及到 `Area 51 Investigation` 的引用。因为：

- The `Investigation Name` parameter is user-provided. No information about the underlying data is exposed to the action type viewer.  
	`Investigation Name` 参数是由用户自定义的。关于底层数据的信息并未对操作类型查看器进行公开展示。
- The `Investigation Object` parameter will respect existing restrictions on object visibility for this user.  
	`Investigation Object` 参数会遵循对该用户对象可见性所设定的限制。

Therefore, neither of these search queries represents a data privacy concern.  
因此，这两个搜索查询都不构成数据隐私方面的问题。

![Object Dropdown Property Filters](https://www.palantir.com/docs/resources/foundry/action-types/objectDropdownSecurityProperty.png)

## Technical details 技术细节

In most cases, the actions backend redacts sensitive information in the action type definition to avoid exposing sensitive property values. For example, action submission criteria are hidden from users who cannot edit action types. Similarly, a user will not be able to see the new object dropdown filters in the action type definition in the interface or while inspecting the response in the backend.  
在大多数情况下，后端会隐藏动作类型定义中的敏感信息，以避免暴露这些敏感属性的值。例如，那些无法编辑动作类型的用户将无法看到动作提交标准。同样，用户在界面中查看动作类型定义时，或者在后端处理响应数据时，也无法看到新的对象下拉过滤选项。

However, when viewing the action form, the object dropdown validation is converted into an object set. This means that users could review the network request containing this object set. In the example above, the user would receive an object set RID containing the `Investigation Name = 'Area 51 Investigation'` filter, revealing the existence of that property value even if they could not view any of its corresponding objects.  
不过，在查看操作表单时，对象下拉菜单的验证功能会转化为一个对象集。这意味着用户可以查看包含该对象集的网络请求。在上面的例子中，用户会收到一个包含 `Investigation Name = 'Area 51 Investigation'` 过滤器的对象集，从而得知该属性的存在，即使他们无法查看与该属性对应的任何对象。

This means that these values will **not be visible in the interface** for any users. If visibility is a greater concern than security, this warning can be ignored.  
这意味着这些值在界面上对于任何用户来说都是不可见的。如果可见性比安全性更重要，那么可以忽略这个警告。

Action types • Parameters • Object dropdown security considerations • Palantir