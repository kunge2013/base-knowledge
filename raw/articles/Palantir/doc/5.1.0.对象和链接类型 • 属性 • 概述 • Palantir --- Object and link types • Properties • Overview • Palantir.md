- Capabilities 能力
	- [AI Platform (AIP)](https://www.palantir.com/docs/foundry/aip/overview/)
		- [Data connectivity & integration](https://www.palantir.com/docs/foundry/data-integration/overview/)
		- [Model connectivity & development](https://www.palantir.com/docs/foundry/model-integration/overview/)
		- [Ontology building](https://www.palantir.com/docs/foundry/ontology/overview/)
		- [Developer toolchain](https://www.palantir.com/docs/foundry/dev-toolchain/overview/)
		- [Use case development](https://www.palantir.com/docs/foundry/app-building/overview/)
		- [Observability](https://www.palantir.com/docs/foundry/observability/overview/)
		- [Analytics](https://www.palantir.com/docs/foundry/analytics/overview/)
		- [Product delivery](https://www.palantir.com/docs/foundry/devops/overview/)
		- [Security & governance](https://www.palantir.com/docs/foundry/security/overview/)
		- [Management & enablement](https://www.palantir.com/docs/foundry/administration/overview/)
- [Getting started 入门指南](https://www.palantir.com/docs/foundry/getting-started/overview/)
- [Architecture center 建筑中心](https://www.palantir.com/docs/foundry/architecture-center/overview/)
- Platform updates 平台更新

[Ontology building](https://www.palantir.com/docs/foundry/ontology/overview/) Object and link types [Properties](https://www.palantir.com/docs/foundry/object-link-types/properties-overview/) [Overview](https://www.palantir.com/docs/foundry/object-link-types/properties-overview/)  
本体构建：构建对象与链接类型属性概述

## Properties 属性

A **property** of an [object type](https://www.palantir.com/docs/foundry/object-link-types/object-types-overview/) is the schema definition of a characteristic of a real-world entity or event. A **property value** refers to the value of a property on an object, or a single instance of that real world entity or event.  
对象类型的属性指的是现实世界中某个实体或事件的特定特征所对应的模式定义。属性值则指对象上该属性的值，或者该现实世界实体或事件的单个实例的值。

For example, in the Ontology Manager, an `Employee` object type may have properties `employee number`, `start date`, and `role`. The notional employee “Melissa Chang” may have property values “11502” for `employee number`, “October 9, 2016” for `start date`, and “software engineer” for `role`.  
例如，在 Ontology Manager 中， `Employee` 对象类型可能具有 `employee number` 、 `start date` 和 `role` 等属性。比如，名为“Melissa Chang”的员工可能具有如下属性值： `employee number` 为“11502”， `start date` 为“2016 年 10 月 9 日”， `role` 为“软件工程师”。

Similarly, in the Ontology Manager, a `Flight` object type may have properties `departure date`, `arrival date`, and `passenger count`. The object “JFK → SFO 24-02-2021” may have property values “24-02-2021” for `departure date`, “25-02-2021” for `arrival date`, and “150” for `passenger count`.  
同样，在 Ontology Manager 中， `Flight` 对象类型可能具有 `departure date` 、 `arrival date` 和 `passenger count` 等属性。例如，对象“JFK → SFO 24-02-2021”可能具有以下属性值： `departure date` 为“24-02-2021”， `arrival date` 为“25-02-2021”， `passenger count` 为“150”。

The concepts underpinning the Ontology have analogous concepts in the structure of a dataset. The definition of a property in the Ontology is analogous to that of a column in a dataset, while the definition of a property value is analogous to that of a field in the dataset. For example, an `Employee` dataset may have columns for `departure date`, `arrival date`, and `passenger count`. In this case, a single field will have the value “11502” for the `employee number` column of the row for employee “Melissa Chang.”  
在 Ontology 中，各种概念与数据集结构中的相应元素相对应。在 Ontology 中，一个属性的定义相当于数据集中的一列，而属性值的定义则相当于数据集中的某个字段。例如，一个 `Employee` 的数据集可能包含 `departure date` 、 `arrival date` 和 `passenger count` 等列。在这种情况下，对于员工“Melissa Chang”的记录，其 `employee number` 列的值将会是“11502”。

Rather than being an abstract data model, the Foundry Ontology maps each ontological concept to an organization's actual data, enabling this data asset to power real-world applications. Property values are created and displayed in user applications by adding backing datasources to an object type in the Ontology Manager. To create property values for properties `employee number`, `start date`, and `role` on objects of type `Employee`, an organization will add backing datasources to the `Employee` object type and feed their employee directory and other enterprise data into the Ontology.  
与其说是一种抽象的数据模型，不如说“Foundry Ontology”是将每个本体概念映射到组织实际数据的一种方式。通过这种方式，这些数据资产能够用于支持现实世界中的各种应用。在用户应用程序中，可以通过在 Ontology Manager 中为某种对象类型添加支持数据源来创建属性值。例如，要为类型为 `Employee` 的对象上的属性 `employee number` 、 `start date` 和 `role` 创建属性值，则需要在 `Employee` 对象类型中添加支持数据源，并将员工的目录以及其他企业数据输入到 Ontology 中。

## Supported property types支持的财产类型

| Property base type 资产的基础类型 | Valid as title key?可以作为标题键使用吗？ | Valid as primary key?作为主键是否有效？ | Notes 备注 |
| --- | --- | --- | --- |
| Commonly used: `String`, `Integer`, `Short`   常用参数： `String` 、 `Integer` 、 `Short` | Yes 是的 | Yes 是的 |  |
| Time-based: `Date`, `Timestamp` 基于时间的： `Date` ， `Timestamp` | Yes 是的 | Discouraged 沮丧的 | Typically, time values are inappropriate as primary keys, due to potentially unexpected collisions / uniqueness based on the storage format differing from the display format. In most cases, we recommend using `String` instead.   通常，时间值不适合作为主键，因为根据存储格式与显示格式的不同，可能会导致意外冲突或唯一性问题。在大多数情况下，我们建议使用 `String` 作为主键。 |
| Number-like: `Boolean`, `Byte`, `Long`   数字类型： `Boolean` 、 `Byte` 、 `Long` | Yes 是的 | Discouraged 沮丧的 | `Boolean` limits your object type to two object instances. `Byte` properties can only be assigned in Actions via an `Integer` parameter, so in most cases we recommend using `Integer` properties instead. `Long` has [representational issues in Javascript ↗](https://www.w3schools.com/js/js_numbers.asp#:~:text=JavaScript%20Numbers%20are%20Always%2064,the%20international%20IEEE%20754%20standard.), so not all frontend libraries and code work well with `Long` values greater than 1e15. In most cases, we recommend using `String` instead.   `Boolean` 限制对象类型仅为两个对象实例。 `Byte` 属性只能通过 Actions 的方式，通过 `Integer` 参数进行赋值，因此在大多数情况下，我们建议使用 `Integer` 属性。 `Long` 在 JavaScript 中存在表示性问题，因此并非所有前端库和代码都能很好地处理大于 1e15 的 `Long` 值。在大多数情况下，我们建议使用 `String` instead。 |
| Float-like: `Float`, `Double`, `Decimal`   浮动型： `Float` 、 `Double` 、 `Decimal` | Yes 是的 | No 没有 |  |
| `Vector` | No 没有 | No 没有 |  |
| `Array` | Yes 是的 | No 没有 | Array properties cannot contain null elements.   数组属性不能包含空元素。   If the inner type of the `Array` is not a valid title property, the `Array` property also cannot be used as the title property.   如果 `Array` 的内部类型不是一个有效的标题属性，那么 `Array` 这个属性也无法被用作标题属性。   Nested arrays are not supported in Object Storage v2.   在 Object Storage v2 中不支持嵌套数组。 |
| `Struct` | No 没有 | No 没有 | Struct properties do not support nesting, and fields cannot be arrays. See the [struct documentation](https://www.palantir.com/docs/foundry/object-link-types/structs-overview/#struct-configuration) for detailed information on supported field types.   结构类型不支持嵌套操作，而且字段也不能是数组。有关支持的各种字段类型的详细信息，请参考结构类型的文档说明。 |
| `Media Reference`, `Time Series`, `Geotemporal Series`, `Attachment` | No 没有 | No 没有 |  |
| `Geopoint` | Yes 是的 | No 没有 | Geopoint values are stored as a comma-separated string in the format `latitude,longitude` (for example, `57.64911,10.40744`).   Geopoint 的值以逗号分隔的字符串形式存储，格式为 `latitude,longitude` （例如， `57.64911,10.40744` ）。   See also [Use geospatial data in the Ontology](https://www.palantir.com/docs/foundry/geospatial/ontology/#points), [Create GeoPoint](https://www.palantir.com/docs/foundry/pb-functions-expression/constructGeoPointV1/), [Convert to Ontology GeoPoint](https://www.palantir.com/docs/foundry/pb-functions-expression/createOntologyGeopointV1/), and [Is valid Ontology GeoPoint](https://www.palantir.com/docs/foundry/pb-functions-expression/isValidOntologyGeopointV1/).   另请参考在本体中使用地理空间数据、创建地理点、转换为本体中的地理点，以及是否为有效的本体地理点相关内容。 |
| `Geoshape` | No 没有 | No 没有 |  |
| `Marking` | No 没有 | No 没有 |  |
| `Cipher` | Yes 是的 | No 没有 |  |

To learn more about base types, see [base types](https://www.palantir.com/docs/foundry/object-link-types/base-types/).  
想要了解更多关于基础类型的信息，请参见基础类型章节。

Object and link types • Properties • Overview • Palantir