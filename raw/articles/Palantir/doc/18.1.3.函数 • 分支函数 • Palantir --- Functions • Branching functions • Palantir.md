- Capabilities 能力
	- [AI Platform (AIP)](https://www.palantir.com/docs/foundry/aip/overview/)
		- [Data connectivity & integration](https://www.palantir.com/docs/foundry/data-integration/overview/)
		- [Model connectivity & development](https://www.palantir.com/docs/foundry/model-integration/overview/)
		- [Ontology building](https://www.palantir.com/docs/foundry/ontology/overview/)
		- [Developer toolchain](https://www.palantir.com/docs/foundry/dev-toolchain/overview/)
		- [Use case development](https://www.palantir.com/docs/foundry/app-building/overview/)
		- [Observability](https://www.palantir.com/docs/foundry/observability/overview/)
		- [Analytics](https://www.palantir.com/docs/foundry/analytics/overview/)
		- [Product delivery](https://www.palantir.com/docs/foundry/devops/overview/)
		- [Security & governance](https://www.palantir.com/docs/foundry/security/overview/)
		- [Management & enablement](https://www.palantir.com/docs/foundry/administration/overview/)
- [Getting started 入门指南](https://www.palantir.com/docs/foundry/getting-started/overview/)
- [Architecture center 建筑中心](https://www.palantir.com/docs/foundry/architecture-center/overview/)
- Platform updates 平台更新

[Ontology building](https://www.palantir.com/docs/foundry/ontology/overview/) [Functions](https://www.palantir.com/docs/foundry/functions/overview/) [Branching functions](https://www.palantir.com/docs/foundry/functions/branching-functions/)  
本体构建 函数 分支函数

## Branching functions

You can develop, publish, and consume functions on a global branch. This is currently supported for TypeScript v1 functions and AIP Logic functions.

## Developing functions

To use Global Branching for TypeScript v1, upgrade your repository template version to version `0.903.0` of the `functions-typescript` child template or higher. Review [the documentation on repository upgrades](https://www.palantir.com/docs/foundry/code-repositories/repository-upgrades/#manual-branch-upgrade) for instructions.  
要在 TypeScript v1 中使用全局分支功能，请将你的仓库模板版本升级到 `functions-typescript` 个子模板的 `0.903.0` 版本或更高版本。有关升级操作的详细说明，请参考仓库升级相关文档。

You can develop a function that depends on changes made to resources on your global branch, such as newly created or modified ontology entities.  
你可以开发一个函数，该函数能够根据全局分支中资源的变化做出响应，比如新创建或修改过的本体实体。

![Developing a new function on a branch.](https://www.palantir.com/docs/resources/foundry/functions/branch-function.png)

When you are ready, publish your function with a **version target** — the stable version published to `main` when the global branch merges. During development on your branch, the function is published as unstable preview versions.  
当你准备好了，就可以发布你的功能了。这些功能的版本将指向一个特定的目标版本——即当全局分支合并后，会发布到 `main` 上的稳定版本。在你们的分支上进行开发期间，这些功能会以不稳定的预览版本形式发布。

![Publishing functions on a branch.](https://www.palantir.com/docs/resources/foundry/functions/branch-function-publish.png)

Once you have successfully published your function on the branch, you can use the new function version on your branch in Workshop, AIP Logic, and function-backed actions. This function version is labeled with the `Branched pre-release` tag. Note that functions published on your branch will not be accessible from other branches, including `main`.  
一旦您成功地在分支上发布了该函数，就可以在 Workshop、AIP 逻辑以及基于函数的动作中使用这个新版本的函数。这个带有 `Branched pre-release` 标签的函数版本可以被使用。请注意，在分支上发布的函数无法被其他分支访问，包括 `main` 分支。

![Using a branched function in Workshop.](https://www.palantir.com/docs/resources/foundry/functions/branch-function-backed-variable.png)

![Using a branched function in actions.](https://www.palantir.com/docs/resources/foundry/functions/branch-function-backed-action.png)

It is currently not possible to depend on branched versions of query functions in TypeScript v1 repositories.  
在 TypeScript v1 的仓库中，目前无法依赖查询函数的分支版本。

As you continue developing on your branch, you can continue publishing function versions to the same version target. As long as your version target remains the same, any resource using your function will automatically pull the latest published version. This lets you iterate quickly without manually updating function references after each publish.

If the version target on the branch changes, you must update all dependents of the function that were using the old version on the branch to use the new version target. This will also be displayed as a [check](https://www.palantir.com/docs/foundry/global-branching/core-concepts/#checks).

## Conflict resolution and rebasing functions

When developing on a branch, you will not automatically receive newer function versions published to `main`. This prevents main development from disrupting your branch work. If newer versions are available on `main`, you will see notifications in function version selectors and Ontology Manager. You can then rebase your function versions to pull in all newer versions from `main`.

![Rebasing functions dialog.](https://www.palantir.com/docs/resources/foundry/functions/rebasing-functions.png)

If your version target gets published to `main` while you are developing, you must select a new version target before merging. Once you do, update any dependents to use the new version target.  
如果在开发过程中，你的版本目标被发布到 `main` ，那么必须在合并之前选择一个新的版本目标。完成这一步骤后，请更新所有依赖项，使其使用新的版本目标。

## Merging 合并

When merging your global branch, modified functions will be published on `main` with the stable version target. Resources using the version published on your branch will automatically start using the new stable version that was published on `main` as part of the merge.  
在合并你的全球分支时，那些已修改的函数会被发布到 `main` 上，并针对稳定版本进行发布。使用你分支上发布的版本的资源，会自动开始使用在 `main` 上发布的新的稳定版本，这是合并过程的一部分。

Foundry only merges your version target into `main` and does not merge other versions published on the branch. Those versions will not exist on `main` after your branch merges.  
该分支只会将你的版本目标合并到 `main` 分支中，而不会合并该分支上其他已发布的版本。当你的分支与 `main` 分支合并后，那些已发布的版本将在 `main` 分支中不再存在。

Functions • Branching functions • Palantir