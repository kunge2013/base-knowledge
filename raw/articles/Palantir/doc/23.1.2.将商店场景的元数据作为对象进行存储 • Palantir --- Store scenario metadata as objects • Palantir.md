- Capabilities 能力
	- [AI Platform (AIP)](https://www.palantir.com/docs/foundry/aip/overview/)
		- [Data connectivity & integration](https://www.palantir.com/docs/foundry/data-integration/overview/)
		- [Model connectivity & development](https://www.palantir.com/docs/foundry/model-integration/overview/)
		- [Ontology building](https://www.palantir.com/docs/foundry/ontology/overview/)
		- [Developer toolchain](https://www.palantir.com/docs/foundry/dev-toolchain/overview/)
		- [Use case development](https://www.palantir.com/docs/foundry/app-building/overview/)
		- [Observability](https://www.palantir.com/docs/foundry/observability/overview/)
		- [Analytics](https://www.palantir.com/docs/foundry/analytics/overview/)
		- [Product delivery](https://www.palantir.com/docs/foundry/devops/overview/)
		- [Security & governance](https://www.palantir.com/docs/foundry/security/overview/)
		- [Management & enablement](https://www.palantir.com/docs/foundry/administration/overview/)
- [Getting started 入门指南](https://www.palantir.com/docs/foundry/getting-started/overview/)
- [Architecture center 建筑中心](https://www.palantir.com/docs/foundry/architecture-center/overview/)
- Platform updates 平台更新

[Ontology building](https://www.palantir.com/docs/foundry/ontology/overview/) [Store scenario metadata as objects](https://www.palantir.com/docs/foundry/ontology/persisted-scenario/)  
在 Ontology 中，将 Store 场景的元数据作为对象进行存储。

## Store scenario metadata as objects将商店场景的元数据作为对象进行存储。

You can define and store metadata of a scenario in the Ontology, where each object represents a scenario, to create a **persisted** scenario.  
你可以在 Ontology 中定义和存储场景的元数据。每个对象都代表一个场景，从而创建出持久化的场景。

When stored in the Ontology, scenarios are shareable and collaborative. Teams can compare multiple persisted scenarios side-by-side, attach rich metadata including descriptions and authorship, and link scenarios to related objects for easy organization. Persisted scenarios inherit Foundry's permissions and governance model, ensuring proper access control, and create a durable audit trail of the decision-making process.  
当存储在 Ontology 中时，这些场景是可以共享和协作的。团队可以同时比较多个已保存的场景，添加包含描述和相关作者信息的丰富元数据，并将场景与相关对象链接起来，以便于管理。已保存的场景遵循 Foundry 的权限管理和控制机制，从而确保正确的访问控制，同时还能保留决策过程的完整审计记录。

## Configure scenario object type配置场景对象类型

To configure the scenario object type in Ontology Manager, follow the steps below:  
要在 Ontology Manager 中配置场景对象类型，请按照以下步骤操作：

1. In Ontology Manager, navigate to the **Object types** tab and select **\+ New object type**.  
	在 Ontology Manager 中，找到“对象类型”标签，然后选择“+ 新建对象类型”。
2. Follow the configuration wizard. At step 3 (**Properties**), choose to **Add** an interface, then search for the **Ontology Scenario** interface. Ensure that **Scenario Reference** is mapped to the primary key of your object type, then complete the remaining steps.  
	按照配置向导的提示进行操作。在第三步（属性），选择“添加接口”，然后找到 Ontology Scenario 接口。确保 Scenario Reference 与你的对象类型的主键相关联，之后完成剩余的步骤即可。  
	  
	![Persist scenario metadata in the Ontology.](https://www.palantir.com/docs/resources/foundry/ontology/persist-scenario-metadata-ontology-manager.png)
3. Once the new object type is created, open the **Properties** tab and confirm that the **Primary Key** property has a value type of **Scenario Reference** assigned to it.  
	一旦新的对象类型被创建出来，请打开“属性”选项卡，确认“主键”属性中已分配了“场景引用”作为其值类型。
4. Optionally, add additional properties to store metadata about the scenario, such as **Created By** or **Description**. Save the configuration and wait for the initial sync to complete.  
	可选地，可以添加额外的属性来存储关于该场景的元数据，例如创建者或描述等。保存配置后，等待初始同步完成。

## Configure create/save action配置创建/保存操作

1. In Ontology Manager, locate your Scenario Object Type. If you did not configure a **Create Object** action during the initial setup, select **\+ New** in the **Action types** card to create one.  
	在 Ontology Manager 中，找到对应的“场景对象类型”。如果您在初始设置时没有配置“创建对象”操作，那么请在“操作类型”卡片中选择“+新建”来创建一个新的对象类型。
2. Within the **Create Object** action configuration, add an additional parameter of type **String**. In the **Constraints** card, set the value type to **Scenario Reference Value**. Then disable the **Visibility** toggle to hide the parameter from end users.  
	在“创建对象”操作配置中，添加一个名为 String 的额外参数。在“约束条件”卡片中，将参数类型设置为“场景参考值”。然后关闭“可见性”选项，以避免该参数被最终用户看到。  
	  
	![Create persist scenario action in the Ontology.](https://www.palantir.com/docs/resources/foundry/ontology/persist-scenario-create-scenario-action-configuration.png)
3. In the **Rules** tab of the Action configuration page, ensure the **Unique ID** property is mapped to the new **Scenario Reference** parameter.  
	在“动作配置”页面的“规则”选项卡中，请确保“唯一 ID”属性已映射到新的“场景参考参数”上。  
	  
	![Create persist scenario action property mapping in the Ontology.](https://www.palantir.com/docs/resources/foundry/ontology/persist-scenario-create-scenario-action-property-mapping-redacted.png)

## Persist an existing temporary scenario维持现有的临时场景

Before proceeding, ensure you have completed the [temporary scenario configuration tutorial](https://www.palantir.com/docs/foundry/ontology/temporary-scenario/). Once your first temporary scenario is in place, follow these steps in Workshop:  
在继续之前，请确保你已经完成了临时场景配置教程的学习。一旦第一个临时场景已经设置完毕，请按照研讨会中的步骤进行操作：

1. Add a **Button Group** widget to your application to handle saving the scenario.  
	在应用中添加一个按钮组组件，用于处理场景的保存操作。
2. Set the button's action to the **Create Object** action configured on your Scenario Object Type.  
	将按钮的操作设置为与您所配置的场景对象类型相关的“创建对象”操作。
3. In the **Default Value** configuration, set the **Scenario Reference** parameter to be populated by the Active Scenario variable generated by the **Scenario Manager** widget.  
	在默认值配置中，将 Scenario Reference 参数设置为由 Scenario Manager 小部件生成的活跃场景变量来填充。  
	  
	![Persist scenario button parameter default.](https://www.palantir.com/docs/resources/foundry/ontology/persist-scenario-button-parameter-default.png)

## Create new scenario object创建新的场景对象。

Make the following adjustments in Workshop to display persistent scenarios:

1. In the **Create or Save Scenario** action, the **Scenario reference** parameter does not require a default value; it will be auto-generated upon action execution.
2. Replace the **Scenario Manager** widget with an **Object List** widget (or any other object display widget) configured to list your persisted Scenario objects.
3. Set the **Scenario Object Type** as the data source for the newly added widget.  
	  
	![Persist scenario object list configuration in the Workshop.](https://www.palantir.com/docs/resources/foundry/ontology/persist-scenario-workshop-object-list-widget.png)
4. Replace the **Active Scenario** variable with an **Object-backed scenario** variable, and set its value to the **Active Object** from the **Object List** widget. You should update the variable selection in other widgets.  
	请将“Active Scenario”变量替换为由对象支持的场景变量，并将其值设置为从对象列表小部件中选择的当前活动对象的值。同时，还需要更新其他小部件中变量的选择。  
	  
	![Persist scenario object backed scenario variable in the Workshop.](https://www.palantir.com/docs/resources/foundry/ontology/persist-scenario-object-backed-scenario-variable.png)

Store scenario metadata as objects • Palantir