- Capabilities 能力
	- [AI Platform (AIP)](https://www.palantir.com/docs/foundry/aip/overview/)
		- [Data connectivity & integration](https://www.palantir.com/docs/foundry/data-integration/overview/)
		- [Model connectivity & development](https://www.palantir.com/docs/foundry/model-integration/overview/)
		- [Ontology building](https://www.palantir.com/docs/foundry/ontology/overview/)
		- [Developer toolchain](https://www.palantir.com/docs/foundry/dev-toolchain/overview/)
		- [Use case development](https://www.palantir.com/docs/foundry/app-building/overview/)
		- [Observability](https://www.palantir.com/docs/foundry/observability/overview/)
		- [Analytics](https://www.palantir.com/docs/foundry/analytics/overview/)
		- [Product delivery](https://www.palantir.com/docs/foundry/devops/overview/)
		- [Security & governance](https://www.palantir.com/docs/foundry/security/overview/)
		- [Management & enablement](https://www.palantir.com/docs/foundry/administration/overview/)
- [Getting started 入门指南](https://www.palantir.com/docs/foundry/getting-started/overview/)
- [Architecture center 建筑中心](https://www.palantir.com/docs/foundry/architecture-center/overview/)
- Platform updates 平台更新

[Ontology building](https://www.palantir.com/docs/foundry/ontology/overview/) Object and link types [Properties](https://www.palantir.com/docs/foundry/object-link-types/properties-overview/) [Edit object type properties](https://www.palantir.com/docs/foundry/object-link-types/edit-properties/)  
ontology 构建对象与链接类型属性 编辑对象类型属性

## Edit object type properties编辑对象类型属性

### Delete a property 删除一个属性

From within the property editor, in the properties pane, hover over the property you want to delete and select **Delete property**.  
在属性编辑器的属性面板中，将鼠标指针移动到想要删除的属性上，然后选择“删除属性”选项。

- Note that the deletion of the property only takes effect after you save your changes, and will break any views or applications referencing the property.  
	请注意，只有保存了更改之后，属性的删除才会生效，而且会使得所有引用该属性的视图或应用程序出现异常。
- Properties with an `active` status **cannot** be deleted.  
	具有 `active` 属性的对象无法被删除。
	- Read more about [statuses](https://www.palantir.com/docs/foundry/object-link-types/metadata-statuses/).  
		了解更多关于状态的信息。

### Change the column backing a property更改属性所依赖的列

From within the property editor, in the properties pane, hover over the property you want to unmap and select **Unlink property**. To link the property to a new column, hover over the property and select **Map to a column**.  
在属性编辑器的属性面板中，将鼠标指针移动到想要解除关联的属性上，然后选择“解除属性关联”选项。如果想要将属性链接到新的列上，请将鼠标指针移动到该属性上，然后选择“链接到列”选项。

![Mapping to a column](https://www.palantir.com/docs/resources/foundry/object-link-types/edit-object-type-map-to-column.png)

### Edit a property type’s metadata编辑属性类型的元数据

You can edit metadata for a property type by selecting the property type, as shown in the image below.  
你可以通过选择某个属性类型来编辑该类型的元数据，如下图所示。

![Edit property metadata](https://www.palantir.com/docs/resources/foundry/object-link-types/edit-object-type-property-metadata.png?width=500)

The available options for editing property metadata are clustered into four different tabs which give access to the following configurations:  
用于编辑属性元数据的选项被集中显示在四个不同的标签页中，这些标签页提供了以下配置选项：

1. **Display name and description:** Select into the existing display name or description to edit the text.  
	显示名称与描述：选择现有的显示名称或描述以编辑其中的文本。
2. **Status:** Select the existing status to open a dropdown of available statuses. Choose from the `deprecated`, `experimental`, and `active` statuses.  
	状态：选择现有的状态以打开下拉菜单，从中可以选择 `deprecated` 、 `experimental` 和 `active` 状态。
	- Read more about [statuses](https://www.palantir.com/docs/foundry/object-link-types/metadata-statuses/).  
		了解更多关于状态的信息。
3. **API name:** Select into the existing API name to change its value.  
	API 名称：选择现有的 API 名称，以便更改其值。
	- Note that you **cannot** change the API name for properties with an `active` status.  
		请注意，对于处于 `active` 状态的对象，无法更改其 API 名称。
		- Read more about [statuses](https://www.palantir.com/docs/foundry/object-link-types/metadata-statuses/).  
			了解更多关于状态的信息。
				- Read more about [valid API names](https://www.palantir.com/docs/foundry/object-link-types/create-object-type/#api-name).  
			了解更多关于有效 API 名称的信息。
4. **Keys:** Indicate whether a property is the object type’s title key or primary key.  
	键：用于表明某个属性是对象的类型键还是主键。
	- Note that you **cannot** change the primary key of an object type with an `active` status.  
		请注意，具有 `active` 状态的对象类型无法更改其主键。
		- Read more about [keys](https://www.palantir.com/docs/foundry/object-link-types/create-object-type/#configure-the-primary-key-and-title-key) and about [statuses](https://www.palantir.com/docs/foundry/object-link-types/metadata-statuses/).  
			了解更多关于键和状态的信息。
5. **Value formatting:** Apply a special formatter to the values of a property to make them more readable in applications.  
	值格式设置：对属性的值应用一种特殊的格式化方式，使得它们在应用程序中更易于阅读。
	- Read more about [value formatters](https://www.palantir.com/docs/foundry/object-link-types/value-formatting/).  
		了解更多关于价值格式化工具的信息。
6. **Conditional formatting:** Apply rules to a property that dictate how it will be rendered in applications.  
	条件格式设置：对某个属性应用规则，以决定其在界面中的显示方式。
	- Read more about [conditional formatting](https://www.palantir.com/docs/foundry/object-link-types/conditional-formatting/).  
		了解更多关于条件格式化的信息。
7. **Property base type:** Select the property’s base type from the dropdown. The type of the property constrains the possible set of operations that can be done on the property’s values.  
	属性基础类型：从下拉菜单中选择属性的基础类型。属性的类型决定了可以对属性值执行哪些操作。
	- For example, a property with base type `timestamp` can be shown in a timeline widget in Object Explorer.  
		例如，一个基础类型为 `timestamp` 的属性可以在对象浏览器的时间轴小部件中显示出来。
		- You will receive an error if the type of a property is not compatible with the type of its backing column.  
		如果某个属性的类型与其对应的后台列的类型不兼容，那么你将收到一个错误。

If you make a change to object property types, you must also update the type expected by Actions that interact with property on that object. To do this, open the Action in Ontology Manager and edit the expected type.  
如果你对对象的属性类型进行了修改，那么必须与那些与该对象属性交互的操作的预期类型也进行更新。为此，请打开 Ontology Manager 中的相关操作界面，并编辑预期的类型。

8. **Type classes:** Apply type classes as additional metadata that can be interpreted by applications.  
	类型类：将类型类作为额外的元数据应用起来，这些元数据可以被应用程序进行解释。
	- See the [type classes documentation](https://www.palantir.com/docs/foundry/object-link-types/metadata-typeclasses/) for a list of available type classes.  
		有关可用类型类的详细信息，请参阅相关文档。
9. **Render hints:** Select render hints from the supplied checklist that you want applied to a property in order to improve how a property value is rendered and indexed into Object Storage v1 (Phonograph).  
	渲染提示：从提供的检查列表中选择所需的渲染提示，将这些提示应用于属性上，以改进属性值在 Object Storage v1 中的渲染和索引方式。
	- See the [render hints documentation](https://www.palantir.com/docs/foundry/object-link-types/metadata-render-hints/) for descriptions of the available render hints.  
		有关可用渲染提示的描述，请参阅渲染提示文档。
10. **Visibility:** Select the existing visibility to open a dropdown of available visibilities. A `prominent` property will lead applications to show this property first to users. A `hidden` property will not appear in user applications.  
	可见性：选择现有的可见性选项，将会弹出一个下拉菜单，其中包含可用的可见性选项。 `prominent` 这样的属性会被应用到应用程序中，使得用户首先看到这个属性。而 `hidden` 这样的属性则不会出现在用户的应用程序界面中。

After making a change to property metadata, initiate a re-index of the affected object to update the Ontology.  
在对属性元数据进行修改后，需要启动受影响对象的重新索引操作，以更新 ontology 数据。

### Bulk edit multiple properties批量编辑多个属性

You can select multiple properties in the property editor by holding the **Cmd/Ctrl** key while selecting properties. Once multiple properties are selected, the following bulk editing actions become available:  
你可以在属性编辑器中选择多个属性，只需同时按住 Cmd/Ctrl 键即可进行选择。当多个属性被选中后，就可以执行以下批量编辑操作了：

- Changing base type.正在更改基础类型。
- Adding/removing of type classes.  
	添加/删除类型类。
- Changing render hints.正在更改渲染提示设置。
- Changing visibility.改变可见性。
- Adding/removing value formatting.  
	添加/删除值格式设置。

![Edit property metadata](https://www.palantir.com/docs/resources/foundry/object-link-types/edit-object-type-bulk-edit-multiple-properties.png)

You can also bulk edit some of the above fields from outside of the property editor, by selecting the **Properties** page from the sidebar of an object type view. Simply select the checkboxes next to the properties you want to edit and a new row will appear at the top of the table with options for bulk editing.  
您也可以从属性编辑器的外部批量编辑某些字段。只需在对象类型视图的侧边栏中选择“属性”页面，然后勾选您想要编辑的属性的旁边的复选框即可。这样，表格顶部会出现新的行，其中包含用于批量编辑的选项。