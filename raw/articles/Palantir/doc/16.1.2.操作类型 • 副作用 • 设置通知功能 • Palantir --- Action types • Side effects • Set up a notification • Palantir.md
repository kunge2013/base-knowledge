- Capabilities 能力
	- [AI Platform (AIP)](https://www.palantir.com/docs/foundry/aip/overview/)
		- [Data connectivity & integration](https://www.palantir.com/docs/foundry/data-integration/overview/)
		- [Model connectivity & development](https://www.palantir.com/docs/foundry/model-integration/overview/)
		- [Ontology building](https://www.palantir.com/docs/foundry/ontology/overview/)
		- [Developer toolchain](https://www.palantir.com/docs/foundry/dev-toolchain/overview/)
		- [Use case development](https://www.palantir.com/docs/foundry/app-building/overview/)
		- [Observability](https://www.palantir.com/docs/foundry/observability/overview/)
		- [Analytics](https://www.palantir.com/docs/foundry/analytics/overview/)
		- [Product delivery](https://www.palantir.com/docs/foundry/devops/overview/)
		- [Security & governance](https://www.palantir.com/docs/foundry/security/overview/)
		- [Management & enablement](https://www.palantir.com/docs/foundry/administration/overview/)
- [Getting started 入门指南](https://www.palantir.com/docs/foundry/getting-started/overview/)
- [Architecture center 建筑中心](https://www.palantir.com/docs/foundry/architecture-center/overview/)
- Platform updates 平台更新

[Ontology building](https://www.palantir.com/docs/foundry/ontology/overview/) [Action types](https://www.palantir.com/docs/foundry/action-types/overview/) [Side effects](https://www.palantir.com/docs/foundry/action-types/side-effects-overview/) [Set up a notification](https://www.palantir.com/docs/foundry/action-types/set-up-notification/)  
构建本体结构，定义动作类型、副作用，以及设置通知功能。

## Set up a notification设置通知功能吧。

This tutorial demonstrates how to set up an action with a notification.  
本教程展示了如何配置一个包含通知功能的操作。

We will be using an action which updates the `Priority` property of an `Alert` object, and also notifies the `Assignee` (a Foundry user) which is stored as a property on that Alert object. If you want to follow along, you'll need to have the following already set up:  
我们将使用一个操作来更新 `Alert` 对象的 `Priority` 属性，同时通知 `Assignee` （即 Foundry 的用户）。这个通知会被存储在该 Alert 对象的相关属性中。如果你想要参与这个过程，那么你需要先配置好以下相关内容：

- An object with the correct properties and configured to be editable via actions  
	一个具有正确属性的对象，并且可以通过操作来编辑。
- An action which takes in one of your objects as well as a parameter containing the new priority and updates the priority property on the specified object. If you previously followed [the tutorial on getting started with actions](https://www.palantir.com/docs/foundry/action-types/getting-started/), you should already have this set up.  
	这是一个操作，它接受一个对象作为参数，该参数包含新的优先级信息，然后更新指定对象的优先级属性。如果你之前已经学习了关于如何使用操作的入门教程，那么你应该已经完成了相关设置。

If you are new to managing objects, you can read about [how to set up an object type](https://www.palantir.com/docs/foundry/object-link-types/create-object-type/).  
如果你是初次使用对象管理功能的人，可以阅读有关如何创建对象类型的介绍。

## Prerequisites 先决条件

### Complete the Getting Started tutorial完成入门教程的学习吧。

This tutorial assumes you already completed the [Getting Started](https://www.palantir.com/docs/foundry/action-types/getting-started/) tutorial for actions.  
本教程假定您已经完成了关于动作的入门教程。

### Add the assignee property to your object type请将“assignee”属性添加到你的对象类型中。

For this tutorial, you will need to have a property on the `Alert` object that is called `Case Managers` and contains the Foundry user ID for the currently assigned user. Typically, if you are using actions to construct your workflow, you will be able to capture and store user IDs with the user selector components in your application. These will show up as full usernames wherever they are displayed in Foundry.  
在这个教程中，你需要在一个名为 `Alert` 的对象上定义一个名为 `Case Managers` 的属性，该属性包含当前已分配用户的 Foundry 用户 ID。通常，如果你使用动作来构建工作流程，你可以利用应用程序中的用户选择器组件来捕获并存储用户 ID。在这些组件中，用户 ID 会以完整的用户名形式显示在 Foundry 系统中。

## Add a notification 添加通知功能

First, navigate to your action that updates the ticket priority. Under the **Rules** section select **Add new rule**, followed by **Notification**. This will open the configuration dialog for adding a notification.  
首先，请找到用于更新工单优先级的操作。在“规则”部分，选择“添加新规则”，然后选择“通知”。这将打开一个配置对话框，以便您可以添加通知功能。

![Recipients configuration](https://www.palantir.com/docs/resources/foundry/action-types/side_effects_notification_tutorial_edit_action_rules.png?width=500)

## Configure recipients 配置收件人信息

For this example, you will send the notification to the assignee, which is stored as a property of the `Alert` object being edited. To do this, use the option "Recipient(s) from property of object parameter" in the **Recipients** dropdown. Select the `Alert` object that is available as a parameter to the action, then select the `Case managers` property when prompted.  
在这个示例中，你需要将通知发送给被指定的接收者。该接收者信息存储在被编辑的 `Alert` 对象的属性中。为此，可以在接收者下拉菜单中选择“从对象属性中获取接收者”选项。然后，选择作为该操作参数的 `Alert` 对象，当提示时选择 `Case managers` 属性即可。

You should see the selected object parameter and property displayed in the **Recipients** section of the configuration. Keep in mind that the recipient must always be a Foundry user ID. If this property contains something else such as string email addresses, no notifications will be sent.  
您应该可以在配置的“接收者”部分看到所选对象的参数和属性。请注意，接收者必须始终是一个 Foundry 用户 ID。如果此属性包含其他内容，例如字符串形式的电子邮件地址，那么就不会发送任何通知。

For testing, you may initially want to configure the action with hardcoded recipient(s) that can be used to validate the logic and notification content is configured as expected.  
在测试阶段，你可以先配置好该动作，使用固定的收件人列表来验证逻辑是否正确，同时确保通知内容也按照预期进行配置。

![hardcoded recipients](https://www.palantir.com/docs/resources/foundry/action-types/side_effects_notification_tutorial_static_test_user.png)

[Learn more about other recipient configuration options.  
了解更多关于其他接收者配置选项的信息。](https://www.palantir.com/docs/foundry/action-types/notifications/#recipients)

## Configure notification content配置通知内容

Next, you will configure the content of the notification by customizing the notification to address the recipient by name and including the old and new priority of the `Alert` object in the content. An example notification configuration is available below.  
接下来，你需要通过自定义通知内容来指定通知的发送方式，例如使用名字来称呼接收者，并在通知内容中注明 `Alert` 对象的旧优先级和新优先级。下面给出了一个通知配置示例。

First, select "Template" from the content options. This is the most straightforward way to configure the content and does not require writing any code.  
首先，从内容选项中选择“模板”。这是配置内容最简单的方法，无需编写任何代码。

For the subject line, enter your desired message. To add a parameter reference, add a forward slash `/` and select the desired parameter from the dropdown list. If your selection is an object parameter, you will be asked to select which property you want to reference.  
在主题行中，输入您希望显示的消息。若需添加参数引用，请在后面加上一个斜杠 `/` ，然后从下拉列表中选择所需的参数。如果所选参数是对象参数，系统会要求您选择要引用的属性。

For the body, enter text that addresses the recipient by name, identifies the user who made a change, and reports the previous and updated status.  
对于“body”字段，请输入以收件人姓名开头的文本，同时注明进行更改的用户，并报告之前的状态与更新后的状态。

As with the object reference in the subject, you can select the "Recipient", "Current User", and any parameter options from the dropdown list in order to generate the correct reference to those user attributes.  
与主题中的对象引用类似，你可以从下拉列表中选择“接收者”、“当前用户”或其他任何相关参数选项，以生成正确的用户属性引用。

[Learn how to generate notification content with more complex requirements.](https://www.palantir.com/docs/foundry/action-types/notifications/#content)

## Configure a link

Finally, you will add a link to the Object View of the specified `Alert` in Object Explorer. Select "Object View" and then select your ticket object parameter from the dropdown. Then, add a label for the link button that reads `View Ticket`.

Now you are ready to save your entire notification configuration:

![Full configuration](https://www.palantir.com/docs/resources/foundry/action-types/side_effects_notification_tutorial_updated_full_finished_configuration.png?width=400)

[Learn more about other types of links that can be configured.  
了解更多关于可以配置的其他类型链接的信息。](https://www.palantir.com/docs/foundry/action-types/notifications/#content)

## Send a test notification发送一个测试通知吧。

To verify, create a test alert with yourself as the assignee. In order to run the action, you will then need to expose the action in Object Explorer or via a button in a Workshop module as described in the [actions documentation](https://www.palantir.com/docs/foundry/workshop/actions-overview/).  
为了进行验证，请创建一个测试警报，并将自己作为执行者。为了执行该操作，你需要通过对象浏览器或工作区模块中的按钮来触发该操作，具体操作方式请参阅相关文档说明。

Once you've made a test change, you should receive both an in-platform push notification and an email notification to the email account specified on your Foundry user profile. Previews for both in-platform and email notifications are displayed within the notification configuration view.  
一旦您完成了测试修改，您将会收到平台内的推送通知，同时也会收到一封邮件通知，该邮件会发送到您在 Foundry 用户资料中指定的电子邮件地址。在通知配置界面中，可以查看平台内通知和邮件通知的预览效果。

If you did not receive an email, it may be because you have email and/or in-platform notifications disabled. You can verify this in **Notifications** under **User Settings**.  
如果您没有收到电子邮件，可能是由于您已禁用电子邮件和/或平台通知功能。您可以在“用户设置”中的“通知”选项中进行验证。

## Next steps 下一步行动

- Explore other optional features, such as [custom content](https://www.palantir.com/docs/foundry/action-types/notifications/#content-components) for when the recipient chooses to receive notifications via email.  
	探索其他可选功能，例如当接收者选择通过电子邮件接收通知时可用的自定义内容。
- Configure complex logic for recipients or content using [functions](https://www.palantir.com/docs/foundry/functions/configure-notifications/).  
	使用各种函数为接收者或内容配置复杂的逻辑。

Action types • Side effects • Set up a notification • Palantir